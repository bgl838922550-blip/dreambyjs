# 自定义媒体库小程序设计规范

## 定位

自定义媒体库小程序是 baiPlay 的独立媒体库类型，不属于 VOD 订阅。它是社区开发者用 JavaScript 编写的影视媒体库适配器：小程序负责把网页资源、资源站、公开索引页等解析成标准媒体页面数据，baiPlay 负责原生界面、导航、搜索、播放器、播放记录、聚合开关和安全边界。

新增媒体库类型：

```swift
case miniLibrary
```

显示名：

```text
自定义媒体库
```

它与 VOD 的边界：

- VOD：主要用于 TMDB/媒体详情页的资源匹配和播放来源补充。
- 自定义媒体库：本身就是完整媒体库，具备首页、分类页、详情页、资源版本、搜索和播放解析。

## 产品页面

一个小程序源最终会在 App 内生成完整的原生页面链路：

```text
自定义媒体库首页
-> 分类/榜单查看页
-> 电影/剧集详情页
-> 资源版本/线路选择页
-> baiPlay 播放器
```

短剧、短视频类媒体库还可以声明原生刷流页面：

```text
自定义媒体库首页
-> 刷短剧/刷短视频页
-> 详情页或播放器
```

页面结构必须围绕影音内容，不允许变成通用网页插件。小程序可以自由解析和组合数据，但返回给 App 的动作只能是媒体页面、媒体详情、分类浏览、搜索、资源版本或播放。

小程序只提供数据、动作和播放解析，不提供页面 UI。首页、分类页、详情页必须由 App 使用现有原生组件渲染：首页和分类区块复用发现页轮播、榜单、横向 shelf、海报墙组件；媒体详情页复用 TMDB 详情页的 hero、元数据、播放按钮、资源入口和相关推荐结构。

## 添加方式

添加入口选择「自定义媒体库」。

支持地址：

```text
https://example.com/library.js
https://example.com/library.json
file:///.../library.js
```

本地导入直接选择 `.js` 文件。远程 `.json` 作为 manifest，远程或本地 `.js` 作为单文件小程序。

## Manifest

Manifest 用于描述小程序能力。单文件 JS 可以通过 `getManifest()` 返回同样结构。

```json
{
  "id": "kkys",
  "name": "可可影视",
  "version": "1.0.0",
  "entry": "https://example.com/kkys-library.js",
  "author": "community",
  "logo": "https://example.com/logo.png",
  "capabilities": {
    "home": true,
    "category": true,
    "detail": true,
    "search": true,
    "resourceVersions": true,
    "playback": true,
    "actionHook": true,
    "resourceMatching": true,
    "resourceMatch": {
      "enabled": true,
      "parameters": [
        "tmdbId",
        "imdbId",
        "tvdbId",
        "title",
        "originalTitle",
        "alternativeTitles",
        "year",
        "runtimeMinutes",
        "mediaType",
        "seasonNumber",
        "episodeNumber",
        "episodeTitle",
        "episodeRuntimeMinutes"
      ]
    }
  },
  "aggregation": {
    "search": true,
    "playbackHistory": true,
    "resourceMatching": true
  },
  "parameters": [
    {
      "name": "url",
      "title": "订阅链接",
      "type": "input",
      "required": true,
      "description": "用户在媒体库首页右上角设置中填写的参数。",
      "value": "https://example.com/index.m3u",
      "placeholders": [
        { "title": "示例", "value": "https://example.com/index.m3u" }
      ]
    },
    {
      "name": "mode",
      "title": "模式",
      "type": "enumeration",
      "value": "default",
      "enumOptions": [
        { "title": "默认", "value": "default" },
        { "title": "快速", "value": "fast" }
      ]
    }
  ]
}
```

`logo` 或 `icon` 用于媒体库入口卡片、小程序首页头部和背景水印。建议使用可公开直连的正方形 PNG/WebP/ICO，App 会按比例缩放，不要求小程序自己处理圆角或裁剪。内容页、详情页、演员头像和剧集 still 图如果需要防盗链请求头，应在对应条目上声明图片 Header；媒体库入口卡片的 `logo/icon` 仍建议使用直连图或 data URL。

`parameters` 用于声明用户可配置参数。App 会在自定义媒体库首页右上角「媒体库设置」中自动生成表单，并把用户填写的值保存到当前媒体库配置里。字段说明：

- `name`: 参数键名，必须稳定，JS 通过 `ctx.params[name]` 读取。
- `title`: 设置页显示名称。
- `type`: 控件类型，支持 `input`、`password`、`number`、`boolean/toggle`、`enumeration/select`、`objectList`。
- `description`: 设置页辅助说明。
- `value` / `defaultValue`: 默认值。
- `required`: 是否必填。App 负责显示必填标识，小程序仍应在接口里校验。
- `placeholders` / `presets`: 输入类参数的快捷填充值。
- `enumOptions` / `options`: 枚举类参数选项。
- `fields`: `objectList` 每一项的字段定义，字段同样支持 `name`、`title`、`type`、`value/defaultValue`、`required`。App 会保存为 JSON，并在调用 JS 时还原为对象数组传入 `ctx.params[name]`。

`objectList` 适合订阅源、账号组、接口列表这类“多条对象配置”。示例：

```js
{
  name: "subscriptions",
  title: "订阅列表",
  type: "objectList",
  fields: [
    { name: "name", title: "名称", type: "input", required: true },
    { name: "url", title: "链接", type: "input", required: true }
  ],
  placeholders: [
    {
      title: "示例订阅",
      value: JSON.stringify([{ name: "示例订阅", url: "https://example.com/live.m3u" }])
    }
  ]
}
```

兼容 Forward 插件结构时，也可以放在 `modules[].params` 中；App 会合并读取为同一套用户参数。

用户设置优先级高于 manifest：

- 「不参与全局资源匹配」关闭聚合搜索/资源匹配，但媒体库内部搜索仍可用。
- 「不聚合播放记录」关闭观看页全局记录聚合，但媒体库内部仍可记录继续观看。

## JS 接口

所有接口可以返回对象或 JSON 字符串，异步接口可以返回 Promise。`ctx` 是 JSON 字符串，社区开发时可用宿主提供的 `argsify(ctx)` 转成对象。App 会把用户填写的 manifest 参数同时注入到 `ctx.params`、`ctx.config`、`ctx.settings` 和 `ctx.parameters`，开发者优先读取 `ctx.params`。

```js
async function getManifest(ctx) {}
async function getHome(ctx) {}
async function getHomeSection(ctx) {} // 可选，首页 section 懒加载
async function getCategory(ctx) {}
async function getDetail(ctx) {}
async function getResourceVersions(ctx) {}
async function resolvePlayback(ctx) {}
async function search(ctx) {}
async function matchResources(ctx) {}
async function onAction(ctx) {}
```

别名兼容：

- `getHome`: `home`
- `getHomeSection`: `homeSection`, `getSection`, `section`, `loadSection`
- `getCategory`: `category`, `catalog`, `list`
- `getDetail`: `detail`
- `getResourceVersions`: `getVersions`, `versions`, `getPlaySources`
- `resolvePlayback`: `resolvePlay`, `play`, `getPlayinfo`
- `search`: `quickSearch`, `getSearch`, `onSearch`
- `matchResources`: `matchMovie`, `matchEpisode`, `matchResource`, `resourceMatch`, `match`
- `onAction`: `handleAction`

## 页面结构

### 首页

首页的数据组织与发现页一致，由顶部轮播和 `sections` 驱动。每个 section 的 `style` 必须使用发现页榜单样式 token；App 会把条目适配为发现页内部的 TMDB-like media item，并复用同一套榜单卡片组件。

`hero` 也可以写成 `carousel` 或 `banners`，App 会统一识别为首页顶部轮播。轮播卡片点击仍使用卡片自己的 `action`，不会跳到 TMDB，而是继续走自定义媒体库详情或分类。

顶部轮播的宽高比例由小程序提供，App 只负责按当前原生轮播卡片宽度计算高度。首页可声明 `heroAspectRatio`，单个轮播条目也可声明 `aspectRatio`；含义都是“宽 / 高”，支持数字 `1.777`，也支持字符串 `"16:9"`、`"16/9"`、`"16x9"`。小程序不要返回像素高度，避免不同设备上布局不一致。

```js
async function getHome(ctx) {
  return {
    "pageType": "home",
    "title": "可可影视",
    "heroAspectRatio": "16:9",
    "hero": [
      {
        "id": "337524",
        "title": "女士优先",
        "type": "movie",
        "poster": "https://example.com/poster.jpg",
        "backdrop": "https://example.com/backdrop.jpg",
        "aspectRatio": "16:9",
        "overview": "简介",
        "action": { "type": "detail", "id": "337524" }
      }
    ],
    "sections": [
      {
        "id": "today",
        "title": "今日更新",
        "style": "discover.ranked",
        "contentType": "movie",
        "more": { "type": "category", "id": "today" },
        "items": []
      }
    ]
  };
}
```

### 发现页榜单样式

小程序必须支持发现页榜单的所有现有样式。`style` 是稳定协议 token，App 按 token 复用现有原生组件；小程序不要自定义卡片布局。`style` 同时作为开发者声明的默认样式，App 会允许用户在同一数据结构兼容的样式组内覆盖该 section 的显示样式。用户覆盖只保存在 App 本地，不会写回小程序脚本。

| token | 原生样式 |
| --- | --- |
| `discover.standard` | 发现页标准横卡 |
| `discover.ranked` | 发现页排名榜单 |
| `discover.spotlight` | 发现页信息横卡 |
| `discover.editorial` | 发现页纵海报信息卡 |
| `discover.posterCompact` | 发现页纵海报简卡 |
| `discover.carousel` | 发现页顶部轮播 |
| `discover.watchProviders` | 发现页播出平台入口 |
| `discover.annualCategories` / `discover.annualClassic` | 豆瓣年度榜单主题卡，适合二级分组、专题、年度榜单、频道入口 |
| `discover.annualPosterStack` | 豆瓣年度榜单海报叠卡 |
| `discover.annualWidePreview` | 豆瓣年度榜单宽幅预览 |
| `discover.annualListPreview` | 豆瓣年度榜单榜单大卡 |
| `media.posterGrid` | 分类页海报墙 |
| `media.episodeGrid` | 剧集格子 |
| `media.resourceVersions` | 资源版本/线路选择 |

未来发现页新增榜单样式时，小程序协议同步新增 token。

普通媒体榜单样式组包括 `discover.standard`、`discover.ranked`、`discover.spotlight`、`discover.editorial`、`discover.posterCompact`。年度/主题入口样式组包括 `discover.annualCategories`、`discover.annualPosterStack`、`discover.annualWidePreview`、`discover.annualListPreview`。App 的样式配置页只会在同组样式之间切换，避免数据结构不匹配。

年度/主题入口样式组的 `items` 应该是“入口卡”而不是普通影片卡，适合“推荐榜单、主题、女优、频道”等二级分组首页。每个入口卡仍然使用标准 `MiniLibraryItem` 字段：`title`、`subtitle`、`poster/backdrop`、`overview`、`badges`、`action`。入口卡应尽量声明 `previewItems`，用子分类第一页或代表榜单里的 3 条真实媒体内容作为预览；App 会像豆瓣年度榜单分类卡一样，把这些内容的海报显示在卡片下半部分，并优先使用第一条预览内容的图片作为入口主图。

```js
{
  "id": "annual-action",
  "title": "动作精选",
  "subtitle": "12 项内容",
  "type": "collection",
  "backdrop": "https://example.com/action.jpg",
  "overview": "浏览动作精选中的代表性影视资源。",
  "badges": ["分类", "榜单"],
  "previewItems": [
    { "id": "m1", "title": "示例影片", "poster": "https://example.com/p1.jpg", "metadataText": "2026" }
  ],
  "action": { "type": "category", "pageId": "annual-action", "title": "动作精选" }
}
```

没有 `previewItems` 时，App 会用入口卡的简介和标签兜底展示；点击卡片始终按入口卡自己的 `action` 处理。开发者如果能用很少请求读取子分类内容，可以在 `getHome/home` 中补齐 `previewItems`；如果需要请求多个子分类，应把 section 标记为懒加载，在 `getHomeSection` 中再补齐，避免阻塞首页进入。

如果首页远端请求较多，必须优先使用 section 懒加载，避免进入媒体库时等待所有榜单完成。`getHome/home` 可以先返回结构和少量静态入口，把需要网络加载的 section 标记为 `lazy: true`，App 会先进入页面并在 section 出现在屏幕附近时调用 `getHomeSection/homeSection/getSection/section/loadSection`：

```js
{
  "id": "home-hot",
  "title": "热门",
  "style": "discover.spotlight",
  "lazy": true,
  "promotesToHero": true,
  "moreAction": { "type": "category", "pageId": "hot", "title": "热门" },
  "items": []
}
```

`getHomeSection(ctx)` 会收到 `ctx.sectionId/id/title/style/pageId/categoryId` 等字段，返回同一个 section 的完整数据即可。返回 section 时应把 `lazy` 改为 `false`。如果远端请求失败，也必须返回合法 section（可以保留原入口卡，或返回 `items: []`），不要返回 `null/undefined`。如果 `promotesToHero: true`，App 会在该 section 加载完成后用前 6 条内容补齐首页顶部轮播。

### 分类/榜单页

分类页参考发现页榜单详情和分类海报墙，支持分页、筛选和排序。
首页 section 标题或右侧箭头点击后进入同一套发现页榜单详情模板：顶部显示榜单标题、来源与数量状态，下方使用自适应海报网格；卡片点击仍按小程序条目的 `action` 进入自定义媒体库详情、分类或搜索。

`getCategory(ctx)` 会收到 `ctx.pageId/id/page/title/sort/sortBy/sort_by`。如果页面需要远端排序，小程序应返回 `sort` 或 `sortOptions`；App 会用这些选项渲染排序菜单，用户切换后重新调用 `getCategory(ctx)`，并把选中的值同时放在 `sort`、`sortBy`、`sort_by`。如果页面只需要本地排序，可以不返回 `sort`。

分类页卡片方向由小程序声明。页面级字段 `itemAspectRatio`、`cardAspectRatio`、`posterAspectRatio`、`gridAspectRatio` 都表示“宽 / 高”，支持数字或 `"16:9"`、`"2:3"`。也可以用 `imageOrientation: "landscape"` 或 `"portrait"`。条目级 `aspectRatio` 优先级更高，适合单个条目特殊比例。

如果入口卡或 `moreAction` 点击后会进入分类页，也建议在 `action/moreAction` 上同步声明 `itemAspectRatio` 或 `imageOrientation`。App 会在分类页首帧使用 action 上的比例，随后再用 `getCategory(ctx)` 返回的页面级比例校正，避免页面先按默认竖向海报渲染再切换为横向海报。

```js
async function getCategory(ctx) {
  const args = argsify(ctx);
  return {
    "pageType": "category",
    "id": args.id,
    "title": "热门电影",
    "style": "discover.spotlight",
    "itemAspectRatio": "16:9",
    "page": args.page || 1,
    "hasMore": true,
    "selectedSortValue": args.sort_by || "latest",
    "filters": [
      { "id": "year", "title": "年份", "values": ["全部", "2026", "2025"] }
    ],
    "sort": [
      { "id": "latest", "title": "最新", "value": "latest" },
      { "id": "hot", "title": "最热", "value": "hot" }
    ],
    "items": []
  };
}
```

### 刷短剧/刷短视频页

短剧、短视频、竖屏片段类小程序可以复用 App 原生刷流页面。开发者只需要声明入口 action 的 `presentation: "shortFeed"`，并让该 action 指向一个普通 `getCategory(ctx)` 页面。App 持有播放队列、当前 index、播放器切换、详情入口、播放记录和低水位加载；小程序只负责返回当前页的媒体条目、媒体信息和播放地址/播放参数。

入口卡示例：

```js
{
  id: "short-feed",
  title: "刷短剧",
  subtitle: "上下滑动连续看",
  type: "collection",
  backdrop: "https://example.com/feed-cover.jpg",
  previewItems: [],
  action: {
    type: "category",
    pageId: "feed",
    title: "刷短剧",
    presentation: "shortFeed",
    itemAspectRatio: "9:16"
  },
  providerIds: {
    MiniLibraryPresentation: "shortFeed"
  }
}
```

`getCategory(ctx)` 返回标准列表即可，建议使用 `pageType: "shortFeed"`、`itemAspectRatio: "9:16"`。每个条目代表一个可直接播放的短剧集或短视频，推荐直接携带 `resourceGroups/versions` 播放地址列表；如果暂时只能返回播放参数，也必须能通过 `resolvePlayback(ctx)` 解析：

```js
async function getCategory(ctx) {
  return {
    pageType: "shortFeed",
    id: "feed",
    title: "刷短剧",
    itemAspectRatio: "9:16",
    page: ctx.page || 1,
    hasMore: true,
    items: [
      {
        id: "episode-1",
        title: "示例短剧",
        subtitle: "第 1 集",
        type: "episode",
        poster: "https://example.com/cover.jpg",
        backdrop: "https://example.com/cover.jpg",
        overview: "短剧简介",
        badges: ["短剧", "竖屏"],
        resourceGroups: [
          {
            id: "quality",
            title: "清晰度",
            versions: [
              {
                id: "episode-access-key",
                name: "高清",
                url: "https://example.com/video.m3u8",
                container: "m3u8",
                headers: {
                  "Referer": "https://example.com/"
                },
                default: true
              }
            ]
          }
        ],
        action: {
          type: "play",
          itemId: "series-1",
          episodeId: "episode-1",
          versionId: "episode-access-key",
          title: "示例短剧 第 1 集"
        }
      }
    ]
  };
}
```

刷流页只是展示方式，不改变播放协议。App 会优先使用条目 `resourceGroups/versions` 中默认版本的 `url/container/headers`，其次使用 `action.url`，最后才调用 `resolvePlayback(ctx)` 获取最终 `url/container/headers`。App 负责选择播放器策略、写播放记录和处理详情跳转。小程序不要把短剧网页嵌入为 WebView，也不要在 JS 中实现手势、播放器控制层或播放记录。

当 App 判断播放队列接近底部时，会继续调用同一个 `getCategory(ctx)` 并把当前播放上下文放在 `ctx.shortFeed` 中。开发者可以忽略这些字段，继续按 `page` 返回下一页；也可以根据当前条目返回更相关的后续内容：

```js
async function getCategory(ctx) {
  const feed = ctx.shortFeed || {};
  // feed.reason: "initial" | "lowWatermark"
  // feed.currentIndex: App 当前队列 index
  // feed.loadedCount: App 已持有条目数量
  // feed.currentItem: 当前播放条目的完整对象
  return {
    pageType: "shortFeed",
    page: ctx.page || 1,
    hasMore: true,
    items: []
  };
}
```

### 卡片模型

```json
{
  "id": "337524",
  "title": "女士优先",
  "subtitle": "2026 / 韩国 / 剧情",
  "type": "movie",
  "poster": "https://example.com/poster.jpg",
  "backdrop": "https://example.com/backdrop.jpg",
  "rank": 1,
  "year": 2026,
  "rating": 7.4,
  "remarks": "HD",
  "aspectRatio": "16:9",
  "imageFit": "fill",
  "imageReferer": "https://example.com/",
  "posterHeaders": {
    "Referer": "https://example.com/",
    "User-Agent": "Mozilla/5.0"
  },
  "badges": ["1080P", "中字"],
  "overview": "简介",
  "action": { "type": "detail", "id": "337524" }
}
```

`type` 支持：

```text
movie, series, season, episode, category, collection, person
```

`imageFit` 控制图片在卡片容器中的显示方式，只支持两种值：

| 值 | 行为 | 适用场景 |
| --- | --- | --- |
| `fill` | 填满容器，保持比例，超出部分裁剪。默认值。 | 影视海报、剧照、普通媒体卡片 |
| `fit` | 图片完整显示在容器内，保持比例，不裁剪。 | 直播台标、站点 logo、透明背景图标 |

小程序可以在条目、入口卡、`previewItems` 上分别声明 `imageFit`。不声明时 App 按 `fill` 处理，保证旧小程序样式不变。`fit` 只改变图片内容模式，不改变 `aspectRatio` 或卡片尺寸；卡片比例仍由 `aspectRatio`、页面级 `itemAspectRatio` 或 App 默认样式决定。

图片请求可以单独声明 HTTP Header，主要用于站点图片防盗链。图片请求头只用于图片下载和缓存，不会传给 `resolvePlayback()`；播放地址仍使用播放解析结果里的 `headers/header`。支持位置包括首页 `hero`、首页 `sections[].items`、懒加载 section、分类页 `items`、入口卡 `previewItems`、详情页主图、演员头像、剧集 still 图和相关推荐条目。

```js
{
  poster: "https://example.com/poster.jpg",
  backdrop: "https://example.com/backdrop.jpg",
  imageHeaders: {
    Referer: "https://example.com/",
    "User-Agent": "Mozilla/5.0"
  },
  posterHeaders: { Referer: "https://example.com/poster-page" },
  backdropHeaders: { Referer: "https://example.com/detail-page" },
  imageReferer: "https://example.com/"
}
```

字段优先级是专用头优先，通用头兜底：`posterHeaders` 用于 `poster/cover`，`backdropHeaders` 用于 `backdrop/background/thumb`，其他图片使用 `imageHeaders`。便捷字段 `imageReferer/posterReferer/backdropReferer` 等同于写入对应 Header 的 `Referer`。Header 名支持常见别名：`Referer/Referrer/http-referer` 会统一成 `Referer`，`User-Agent/ua` 会统一成 `User-Agent`。App 会把 Header 纳入图片缓存键，避免同一个图片 URL 在不同 Referer 下串图。

推荐示例：

```js
const IMAGE_HEADERS = {
  Referer: "https://example.com/",
  "User-Agent": "Mozilla/5.0"
};

function mediaItem(raw) {
  return {
    id: raw.id,
    title: raw.title,
    poster: raw.poster,
    backdrop: raw.backdrop,
    imageHeaders: IMAGE_HEADERS,
    action: { type: "detail", itemId: raw.id }
  };
}
```

### 动作模型

卡片点击由小程序声明，App 执行。

```json
{ "type": "detail", "id": "337524" }
{ "type": "category", "id": "movie" }
{ "type": "play", "id": "337524" }
{ "type": "category", "pageId": "feed", "presentation": "shortFeed" }
{ "type": "custom", "name": "openRank", "payload": { "rank": "weekly" } }
```

`custom` 会调用：

```js
async function onAction(ctx) {
  return { "type": "category", "id": "weekly" };
}
```

`onAction` 返回结果仍必须是媒体动作，不能请求打开任意网页、执行 native 能力或跳出影音边界。

`presentation` 是可选展示提示，目前支持 `shortFeed`。它只影响 App 用哪种原生模板呈现目标页面，不改变 `getCategory/getDetail/resolvePlayback` 的数据协议。兼容写法包括 `shortFeed`、`short-feed`、`short_feed`。

### 详情页

详情页的数据组织与 TMDB 电影和剧集详情页一致。`title/name`、`poster`、`backdrop`、`overview`、`year`、`rating`、`genres`、`cast`、`seasons`、`recommendations` 都按媒体详情模型返回，App 复用 TMDB 详情页的视觉层级和交互组织。

```js
async function getDetail(ctx) {
  const args = argsify(ctx);
  return {
    "pageType": "detail",
    "id": args.id,
    "type": "series",
    "title": "庆余年",
    "originalTitle": "",
    "year": 2019,
    "poster": "https://example.com/poster.jpg",
    "backdrop": "https://example.com/backdrop.jpg",
    "detailImageAspectRatio": "16:9",
    "logo": "https://example.com/logo.png",
    "imageHeaders": { "Referer": "https://example.com/" },
    "posterHeaders": { "Referer": "https://example.com/poster-page" },
    "backdropHeaders": { "Referer": "https://example.com/detail-page" },
    "logoHeaders": { "Referer": "https://example.com/" },
    "overview": "简介",
    "rating": 8.0,
    "runtime": 45,
    "genres": ["剧情", "古装"],
    "externalIds": { "tmdb": "123", "imdb": "tt123" },
    "cast": [
      {
        "id": "p1",
        "name": "演员",
        "role": "角色",
        "avatar": "https://example.com/a.jpg",
        "avatarReferer": "https://example.com/",
        "action": { "type": "category", "id": "actor-p1", "pageId": "actor-p1", "title": "演员" }
      }
    ],
    "seasons": [
      {
        "id": "s1",
        "title": "第 1 季",
        "index": 1,
        "episodes": [
          {
            "id": "e1",
            "title": "第 1 集",
            "index": 1,
            "poster": "https://example.com/still.jpg",
            "stillReferer": "https://example.com/"
          }
        ]
      }
    ],
    "recommendations": [],
    "resourceSummary": {
      "versionCount": 4,
      "episodeCount": 46,
      "defaultVersionId": "ff"
    }
  };
}
```

电影详情可以没有 `seasons`，直接通过资源版本播放。剧集详情以 `seasons[].episodes[]` 驱动选集。

详情页顶部图优先使用 `backdrop/background/thumb`，再兜底 `poster/cover`。如果小程序能提供详情页专用大图，应把它放到 `backdrop`，并用 `detailImageAspectRatio`、`imageAspectRatio`、`backdropAspectRatio` 或 `heroAspectRatio` 声明原始宽高比。App 会用该比例计算详情页首屏图片高度，图片加载完成后继续按真实图片尺寸校正。

`cast/people/actors` 支持字符串数组，也支持对象数组。演员对象如果带 `action`，App 会把演员卡渲染为可点击入口；影音媒体库里推荐使用 `category` action 打开该演员、导演、频道或标签对应的榜单页。演员页仍然走 `getCategory(ctx)`，可以单独声明排序和卡片方向。

详情页同样支持图片请求头：`imageHeaders` 是详情页通用图片头，`posterHeaders/backdropHeaders/logoHeaders` 分别覆盖海报、顶部大图和 logo。演员对象支持 `imageHeaders/avatarHeaders/profileHeaders` 与 `imageReferer/avatarReferer/profileReferer`；剧集对象支持 `imageHeaders/posterHeaders/stillHeaders` 与 `imageReferer/posterReferer/stillReferer`。

### 资源版本

资源版本是独立模型，用于电影多版本、剧集多线路、多清晰度和可用性声明。

```js
async function getResourceVersions(ctx) {
  const args = argsify(ctx);
  return {
    "itemId": args.itemId,
    "seasonId": args.seasonId,
    "episodeId": args.episodeId,
    "groups": [
      {
        "id": "line",
        "title": "播放线路",
        "versions": [
          {
            "id": "ff",
            "title": "FF线路",
            "quality": "1080P",
            "sourceName": "FF",
            "availability": "playable",
            "ext": { "url": "/play/337524-ff-1.html" }
          },
          {
            "id": "appOnly",
            "title": "4K线路",
            "quality": "4K",
            "availability": "unavailable",
            "reason": "仅 App 端观看"
          }
        ]
      }
    ]
  };
}
```

`availability` 支持：

```text
playable, unavailable, requiresResolve, appOnly
```

### 播放解析

```js
async function resolvePlayback(ctx) {
  const args = argsify(ctx);
  return {
    "url": "https://example.com/index.m3u8",
    "container": "m3u8",
    "headers": {
      "User-Agent": "Mozilla/5.0",
      "Referer": "https://example.com/"
    },
    "subtitles": [],
    "danmaku": null,
    "startPosition": 0,
    "isLive": false,
    "streamKind": "vod"
  };
}
```

App 拿到结果后调用播放器，JS 不直接控制播放器。`resolvePlayback` 只描述媒体资源本身：

- `url`：必填，最终可播放地址。
- `container` / `format`：建议填写，如 `m3u8`、`mp4`、`ts`、`flv`。
- `headers` / `header`：播放请求需要的 HTTP 头。
- `startPositionSeconds` / `startTime` / `startPosition`：起播位置，单位秒。
- `isLive` / `live`：是否直播/线性流。
- `streamKind` / `mediaKind` / `contentKind` / `type` / `mediaType`：媒体性质，建议使用 `vod`、`live`、`movie`、`series`、`episode`。

播放策略由 App 端根据 `url`、`container`、`headers`、`isLive`、媒体探测结果和运行时失败情况统一选择。JS 不应返回或依赖以下字段：`preferDirectAVPlayer`、`forceDirectAVPlayer`、`playbackStrategy`、`strategy`、`forceStrategy`、`playerStrategy`。这些字段为保留/废弃字段，App 在自定义媒体库链路中会忽略。

直播源示例：

```js
async function resolvePlayback(ctx) {
  return {
    url: "https://example.com/live/playlist.m3u8",
    container: "m3u8",
    headers: {
      "User-Agent": "Mozilla/5.0",
      "Referer": "https://example.com/"
    },
    isLive: true,
    streamKind: "live"
  };
}
```

### 资源匹配

资源匹配用于 TMDB 电影/剧集详情页的「匹配资源」能力。小程序如果希望参与 TMDB 详情页资源匹配，必须在 manifest 中声明：

```json
{
  "capabilities": {
    "resourceMatching": true,
    "resourceMatch": {
      "enabled": true,
      "parameters": ["tmdbId", "imdbId", "title", "year", "mediaType"]
    }
  },
  "aggregation": {
    "resourceMatching": true
  }
}
```

`capabilities.resourceMatching`、`supportsResourceMatching`、`resourceMatching`、`matchable` 任一声明为 `true` 即表示支持。`capabilities.resourceMatch` 用于说明小程序实际支持哪些匹配参数，App 会尽量传全，开发者可以只使用自己支持的字段。

当前资源匹配接口作为自定义媒体库标准能力保留；App 暂不在 TMDB 电影/剧集详情页自动调用自定义媒体库做资源匹配。后续接入时仍按本节协议传参与返回。

App 调用 `matchResources(ctx)` 时会传入：

```json
{
  "mode": "resourceMatch",
  "aggregate": true,
  "mediaType": "movie",
  "type": "movie",
  "tmdbId": 12345,
  "tmdbID": 12345,
  "imdbId": "tt1234567",
  "imdbID": "tt1234567",
  "tvdbId": "7654321",
  "tvdbID": "7654321",
  "providerIds": {
    "tmdb": "12345",
    "imdb": "tt1234567",
    "tvdb": "7654321"
  },
  "title": "影片名",
  "name": "影片名",
  "originalTitle": "Original Title",
  "originalName": "Original Title",
  "alternativeTitles": ["别名一", "别名二"],
  "searchTitles": ["影片名", "Original Title", "别名一"],
  "titles": ["影片名", "Original Title", "别名一"],
  "year": 2026,
  "releaseYear": 2026,
  "runtimeMinutes": 126,
  "runtime": 126,
  "duration": 126
}
```

剧集单集匹配会额外传：

```json
{
  "mediaType": "tv",
  "seasonNumber": 1,
  "season": 1,
  "episodeNumber": 3,
  "episode": 3,
  "episodeTitle": "第三集标题",
  "episodeName": "第三集标题",
  "episodeRuntimeMinutes": 45
}
```

返回值可以是标准小程序条目数组，也可以是带 `results` 的对象。App 会用返回条目的 `id/action.itemId` 进入详情，再调用 `getResourceVersions` 获取资源版本；如果返回值已经包含 `resourceGroups`，App 会直接使用。

```json
{
  "results": [
    {
      "id": "/movie/312309.html",
      "title": "女士优先",
      "type": "movie",
      "poster": "https://example.com/poster.jpg",
      "year": 2026,
      "providerIds": {
        "tmdb": "12345",
        "imdb": "tt1234567"
      },
      "action": {
        "type": "detail",
        "itemId": "/movie/312309.html"
      },
      "resourceGroups": [
        {
          "id": "online",
          "title": "在线播放",
          "versions": [
            {
              "id": "line-1",
              "name": "HD",
              "container": "m3u8",
              "action": {
                "type": "play",
                "itemId": "/movie/312309.html",
                "versionId": "line-1"
              }
            }
          ]
        }
      ]
    }
  ]
}
```

电影可以实现 `matchMovie(ctx)`，剧集可以实现 `matchEpisode(ctx)`；如果只实现通用 `matchResources(ctx)`，App 也会自动调用。匹配接口应尽量优先使用 TMDB/IMDb/TVDB ID，其次使用标题、原名、别名、年份和时长。不要在匹配接口里直接返回网页 HTML，必须返回结构化 JSON。

用户设置优先级仍然最高：如果用户开启「不参与全局资源匹配」，即使 manifest 声明支持资源匹配，App 也不会在 TMDB 详情页调用该小程序。

### 搜索

媒体库内部搜索和全局聚合搜索使用同一接口。小程序需要在 manifest 中声明搜索能力，App 才会在小程序首页、分类页、详情页显示搜索按钮，并在统一搜索页的媒体库来源里显示该小程序。

```json
{
  "capabilities": {
    "search": true
  }
}
```

如果 `capabilities.search`、`supportsSearch` 或 `searchable` 为 `false`，或者没有声明搜索能力，App 会隐藏小程序内部搜索入口，也不会在统一搜索页的来源选择器中展示该小程序。

```js
async function search(ctx) {
  const args = argsify(ctx);
  return {
    "pageType": "search",
    "keyword": args.keyword,
    "page": args.page || 1,
    "hasMore": true,
    "items": []
  };
}
```

App 调用搜索 Hook 时会同时传入多组等价字段，方便兼容不同社区写法：

```json
{
  "query": "女士优先",
  "keyword": "女士优先",
  "text": "女士优先",
  "page": 1
}
```

Hook 函数名支持 `search(ctx)`、`quickSearch(ctx)`、`getSearch(ctx)`、`onSearch(ctx)`；返回值可以是 `items` 数组，也可以是 `pageType: "search"` 的标准页面。搜索结果点击后必须能回到小程序详情页，因此条目应提供详情 action：

```json
{
  "id": "312309",
  "title": "女士优先",
  "type": "movie",
  "poster": "https://example.com/poster.jpg",
  "year": 2026,
  "action": {
    "type": "detail",
    "itemId": "312309"
  }
}
```

小程序页面内也可以用搜索 action 调起统一搜索页。App 会自动把搜索来源定位到当前小程序；如果 action 里带 `query`、`keyword`、`searchText` 或 `text`，会进入搜索页后立即搜索：

```json
{
  "type": "search",
  "query": "韩国高分剧集",
  "title": "搜索韩国高分剧集"
}
```

聚合搜索时 App 还可能传：

```json
{
  "keyword": "女士优先",
  "mode": "aggregate",
  "mediaType": "movie",
  "year": 2026,
  "page": 1
}
```

小程序应尽量返回真实片名、年份、类型、海报和详情 action，便于 App 去重、排序和展示。用户如果在统一搜索页手动选择某个小程序媒体库，也会走同一套搜索 Hook，并在点击结果后进入对应的小程序详情模板。

## 播放记录

播放记录由 App 写入，不允许 JS 直接写库。

记录 key：

```text
miniLibrary:{serverId}:{itemId}:{seasonId?}:{episodeId?}:{versionId?}
```

记录内容包含：

- 媒体库 ID 和名称
- item/season/episode/version
- 标题、海报、类型、年份
- 播放进度 ticks
- 更新时间

开启「不聚合播放记录」后，记录仍用于该媒体库内部继续观看，但不进入观看页全局记录聚合。

## 宿主能力

小程序运行在 JavaScriptCore 沙箱内。宿主提供：

```js
$http.get(url, options)
$http.post(url, body, options)
$html.select(html, selector)
$html.text(html)
$html.attr(html, name)
$cache.get(key)
$cache.set(key, value)
$cache.remove(key)
$crypto.md5(text)
$crypto.sha1(text)
$crypto.aesDecrypt(value, key, options)
$crypto.aesDecryptBase64(value, key, options)
$browser.fetch(url, options)
Widget.browser.fetch(url, options)
Widget.tmdb.search(options)
Widget.tmdb.trending(options)
Widget.tmdb.detail(options)
Widget.tmdb.imageURL(path, size)
Widget.vod.sites(options)
Widget.vod.cards(options)
Widget.vod.tracks(options)
Widget.vod.play(options)
Widget.vod.search(options)
$utils.absoluteURL(base, path)
$utils.base64Encode(text)
$utils.base64Decode(text)
$log.info(value)
```

`Widget.browser.fetch()` 用于必须由浏览器环境执行的页面，例如 Cloudflare 验证后的页面、播放器校验脚本、依赖 `sessionStorage/localStorage/canvas` 的播放页。默认 `visible: false`，App 会隐藏执行；只有检测到真实 Cloudflare/Turnstile 验证页时才允许显示浏览器验证界面。返回字段包含 `data`/`html`、`currentURL`、`sessionStorage`、`localStorage`、`capturedRequests`、`mediaSources`、`headers.Cookie`。普通列表、分类、详情页仍应优先使用 `Widget.http.get()`，避免把浏览器作为常规抓取器。站点专用等待条件由小程序自己声明，例如 `waitForSessionStorageKey`、`waitForLocalStorageKey`、`waitForMediaSource`、`waitForAny`；App 只负责通用浏览器执行和最终播放器调起。

示例：

```js
const result = Widget.browser.fetch(playerURL, {
  visible: false,
  timeout: 45,
  waitAfterLoad: 0.5,
  waitForSessionStorageKey: 'vkey',
  headers: { Referer: detailURL }
});
const vkey = JSON.parse(result.sessionStorage.vkey || '{}');
```

`$crypto.aesDecrypt()` 给需要复刻网页播放器解密逻辑的小程序使用。默认按 `AES-CBC-PKCS7` 处理，`value` 默认是 UTF-8；`aesDecryptBase64()` 等同于 `inputEncoding: 'base64'`、`outputEncoding: 'utf8'`。`options` 支持 `iv`、`mode`（`CBC`/`ECB`）、`inputEncoding`（`utf8`/`base64`/`hex`）、`keyEncoding`、`outputEncoding`。`Widget.crypto` 与 `$crypto` 指向同一组能力。

```js
const url = $crypto.aesDecryptBase64(encrypted, key, {
  iv,
  mode: 'CBC'
});
```

`Widget.tmdb` 复用 App 内置 TMDBService，包括 App 当前的 TMDB 直连/代理设置和图片 URL 规则。它适合给小程序做轻量的标题检索、趋势片单、详情补全和资源匹配辅助，不需要小程序自己保存 TMDB token。

```js
const movies = Widget.tmdb.search({
  type: 'movie',      // movie / tv / series，不传则多类型搜索
  query: 'Inception',
  page: 1
}).results;

const trendingTV = Widget.tmdb.trending({
  type: 'tv',
  timeWindow: 'week',
  page: 1
}).results;

const detail = Widget.tmdb.detail({
  type: 'movie',
  id: 27205
});
```

返回条目是轻量结构，常用字段包括：`id/tmdbId`、`title/displayTitle`、`mediaType`、`overview`、`year`、`rating`、`poster`、`backdrop`、`posterPath`、`backdropPath`。`detail()` 会额外尽量返回 `originalTitle/originalName`、`runtimeMinutes`、`genres`、`imdbId`、`tvdbId`、`seasons` 等字段。

`Widget.vod` 复用 App 内置 VODService，用于把已有 XPTV/Forward Widgets/TVBox `type=3` JS 订阅桥接到自定义媒体库。适用场景是：开发者想把一个 `sites + ext JS` 订阅拆成媒体库首页、分类页、详情页和播放线路，但不想在小程序里重新实现 `createCheerio/argsify/getCards/getTracks/getPlayinfo` 运行环境。

```js
const subscriptionURL = 'https://example.com/all.json';
const sites = Widget.vod.sites({ subscriptionURL }).sites;

const page = Widget.vod.cards({
  subscriptionURL,
  siteKey: sites[0].id,
  page: 1
});

const groups = Widget.vod.tracks({
  subscriptionURL,
  siteKey: sites[0].id,
  card: page.items[0]
}).groups;

const playback = Widget.vod.play({
  subscriptionURL,
  siteKey: sites[0].id,
  card: page.items[0],
  track: groups[0].tracks[0],
  groupTitle: groups[0].title
});
```

常用返回字段：

- `sites()`：`sites/list`，每个站点包含 `id/key/title/name/api/ext/searchable/isJSSite`。
- `cards()` / `search()`：`items/list`，每个条目包含 `id/vod_id/title/name/poster/pic/remarks/year/ext/extJSON`。
- `tracks()`：`groups/list`，每组包含 `title/tracks`；每个 track 包含 `id/name/url/ext/extJSON`。
- `play()`：`url/container/headers`，可直接转成 `resolvePlayback()` 的返回值；播放器策略仍由 App 端根据资源信息统一判断。

不提供：

- 任意文件系统访问
- 系统剪贴板
- 任意 native bridge
- 打开外部 App
- 注入 App UI

## 校验规则

添加源时至少校验：

1. 地址可读。
2. manifest 或 `getManifest()` 可解析。
3. `getHome()` 返回至少一个合法 `hero`、`section` 或 `item`。
4. 如果声明支持搜索，`search()` 必须能返回标准 search page。
5. 如果声明支持播放，`getResourceVersions()` 和 `resolvePlayback()` 的返回字段必须满足规范。

## 示例

完整示例见：

- [mini-library-sample.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/mini-library-sample.js)
- [kkys04-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/kkys04-mini-library.js)
- [reference-tmdb-cinema-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/reference-tmdb-cinema-mini-library.js)
- [reference-tmdb-series-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/reference-tmdb-series-mini-library.js)
- [reference-animation-family-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/reference-animation-family-mini-library.js)
- [reference-live-channels-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/reference-live-channels-mini-library.js)
- [reference-shortform-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/reference-shortform-mini-library.js)
- [xptv-forward-vod-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/xptv-forward-vod-mini-library.js)
- [libvio-agc-mini-library.js](/Users/baiguangli/Pictures/baiPlay/docs/examples/libvio-agc-mini-library.js)

本地快速校验 5 个参考源：

```bash
node scripts/validate_mini_library_examples.js
```
